apiVersion: v1
kind: Service
metadata:
  name: cservice
spec:
  type: NodePort
  ports:
    - port: 8000
      protocol: TCP
      nodePort: 30800
    - port: 9000
      protocol: TCP
      nodePort: 30900
  selector:
    app: cservice
